namespace G
{
	/**
	 * Specifies type of change that happened in contract for binding
	 * 
	 * @since 0.1
	 */
	public enum ContractChangeType
	{
		/**
		 * Binding was added
		 * 
		 * @since 0.1
		 */
		ADDED,
		/**
		 * Binding was removed
		 * 
		 * @since 0.1
		 */
		REMOVED
	}
}
